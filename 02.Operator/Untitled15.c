#include<stdio.h>
int main()
    {
        int a;
        float b;
        double c;
        char x;

        printf("Size of int in bytes: %d\nSize of float in bytes: %d\nSize of double in bytes: %d\nSize of Char in bytes: %d\n",sizeof(a),sizeof(b),sizeof(c),sizeof(x));
    }
