#include<stdio.h>
int main()
    {
        int x,y;
        scanf("%d%d",&x,&y);

        if(x>y)
        {
            printf("Max: %d",x);
        }
        else{
            printf("Max: %d",y);
        }

    }
