#include<stdio.h>
int main()
    {
        double u,a,t,s;
        printf("Enter the values of u,t,a = ");
        scanf("%d %d %d",&u,&a,&t);
        s = u*t+0.5*a*t*t;
        printf("s = %d",s);

    }
